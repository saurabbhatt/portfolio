/**
 * Application configuration settings
 */
const config = {
  // Feature flags
  features: {
    // Enable/disable the loading animation
    loaderGif: true,
    
    // Loading duration in milliseconds (only used when loaderGif is true)
    loaderDuration: 1000,
  }
};

export default config; 